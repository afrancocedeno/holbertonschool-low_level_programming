#include "holberton.h"

/**
 * _abs - displays de absolute number
 * @a: as input from sourcefile.c
 * Return: statement
 */
int _abs(int a)

{

	if (a < 0)
	{
		a = -a;
		return (a);
	}
	else
	{
		return (a);
	}
}
