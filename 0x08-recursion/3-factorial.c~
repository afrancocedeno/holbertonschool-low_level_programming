#include "holberton.h"

/**
 * factorial - Entry point function.
 * @n:Input variable.
 */
int factorial(int n)
{
	if (n == 1)
		return (1);
	else if (n < 1)
		return (-1);
	return (n * factorial(n - 1));
}
